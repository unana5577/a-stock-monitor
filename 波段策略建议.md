# 波段策略建议（趋势跟随 · 清仓派）

> **策略定位**：3–6 个月 ETF 波段操作 / 趋势跟随 / 过热即清仓  
> **标的池**：9 只 ETF（sh512400/s512480/sh515120/sh515880/sh516010/sh516160/sh516510/sh562500/sh563530）  
> **底层信号**：基于现有 `m1_sector_lifecycle.py` 的动能/资金行为/阶段输出

---

## 一、整体架构（4 层）

```
┌─────────────────────────────────────────────────────────────┐
│  Portfolio 层（前端 ui_m1.js — 仓位/金额/板块约束）          │
│  输入：Policy 输出 + simState(现金/持仓/板块上限)            │
│  输出：目标仓位、交易金额/份额、确认弹窗                    │
├─────────────────────────────────────────────────────────────┤
│  Policy 层（新增：treasolo/m1_policy.py — 波段状态机）       │
│  输入：Signal 日序列 + 持仓状态                              │
│  输出：trend_state(IN/OUT/COOLING) / action / reason       │
├─────────────────────────────────────────────────────────────┤
│  Signal 层（已有：treasolo/m1_sector_lifecycle.py）          │
│  输出：动能 / 资金行为 / 阶段 / 指标数据（每只ETF每日）     │
├─────────────────────────────────────────────────────────────┤
│  Feature 层（已有 + 新增MA20系列）                           │
│  输入：日线 daily.jsonl                                     │
│  输出：MA/Alpha/AmountShare/Bias 等特征                     │
└─────────────────────────────────────────────────────────────┘
```

**设计原则**：
- 每层独立可验证——任何一层改动能单独回测
- Signal 层保持对"今天发生了什么"的解释性
- Policy 层提供"基于连续性我该做什么"的可执行信号
- Portfolio 层只做资金分配与约束

---

## 二、Feature 层（新增 MA20 资金热度指标）

### 2.1 现有指标（保留）

| 字段 | 含义 |
|------|------|
| `Amount_Share` | 当日成交占全市场比例 |
| `Amount_Share_MA5` | 5日均值 |
| `Amount_Share_Change` | 相对 MA5 的变化率 |

### 2.2 新增指标

| 字段 | 含义 | 计算方式 |
|------|------|----------|
| `Amount_Share_MA20` | 20日成交占比均值 | `rolling(20).mean()` over `amount_share_series` |
| `Amount_Share_Change20` | 相对 MA20 的变化率 | `Amount_Share / Amount_Share_MA20 - 1` |
| `Amount_Share_DaysUp` | hot_连续抬升天数 | 连续满足 `Amount_Share_MA20(t) > MA20(t-1)` 的天数 |

### 2.3 计算位置

在 `treasolo/m1_sector_lifecycle.py` 的 `analyze_sector()` 函数中追加计算（该函数已有 `amount_share_series`），新增的 MA20 字段加入 `指标数据` 输出字典。

```python
# 在 analyze_sector() 中 amount_share_ma5_pct 计算后追加：

# --- 新增：20日资金热度 ---
amount_share_ma20 = np.mean(amount_share_series[-20:]) if len(amount_share_series) >= 20 \
    else np.mean(amount_share_series) if amount_share_series else 0
amount_share_change20 = amount_share_pct / amount_share_ma20 - 1 if amount_share_ma20 else None
```

---

## 三、Signal 层（保持不变）

现有 `treasolo/m1_sector_lifecycle.py` 的 `analyze_sector()` 输出结构：

```
{
  symbol, asof_date,
  动能: "强势向上" / "偏强向上" / "中性震荡" / "弱势反弹" / "弱势向下" / "偏强向下" / "强势向下",
  资金行为: "放量启动" / "横盘整理" / "加速赶顶" / "资金撤退" / "恐慌出逃" / "超跌反弹" / "主线逼空",
  位置区域, 位置名称, 阶段信号,
  操作建议, 显示名称, 含义,
  指标数据: { Alpha_5, Alpha_20, Amount_Share, Amount_Share_MA5, Amount_Share_Change, 
              Amount_Share_MA20, Amount_Share_Change20, Amount_Share_DaysUp,  // 新增 ↑
              MA5, MA20, MA60, MA5_Slope, Bias_20, Bias_20_History_Max, Bias_20_History_Min,
              Pct, Pct_P80, close, ... }
}
```

**关键点**：Signal 层不改任何判定逻辑，只在北京时间交易日计算一次（盘后或盘中），输出单日快照。

---

## 四、Policy 层（波段状态机——核心新增）

### 4.1 状态定义

| 状态 | 含义 | 允许的动作 |
|------|------|-----------|
| **OUT** | 空仓/未入场 | 只有在满足入场条件时进入 `IN` |
| **IN** | 持仓中 | HOLD（不动）/ EXIT（清仓） |
| **COOLING** | 清仓后冷却期 | NONE（禁止入场） |

### 4.2 状态转移规则表

#### 【规则 1】入场：OUT → IN

触发条件（**全部同时满足**）：

```
① 20 日资金热度连续抬升 ≥ 3 个交易日
    即：Amount_Share_MA20(t) > MA20(t-1) > MA20(t-2) > MA20(t-3)

② 当日热度高于 20 日均值
    即：Amount_Share_Change20(t) > 0

③ 动能方向健康
    即：动能 ∈ { "偏强向上", "强势向上" }

④ 不在过热区
    即：Bias_20 < 0.9 × Bias_20_History_Max（仅当 Bias_20 > 0 时判断）
       若 Bias_20 ≤ 0，直接忽略本条约束
```

#### 【规则 1a】建仓时机：信号日入场 + 趋势内回调加仓

**只做右侧，不做左侧**。左侧交易（下跌中提前抄底等反转）存在两个致命问题：
- 等待反转的时间不确定，3–6 个月波段周期内资金可能被锁死数月无收益
- 反转失败则变为"越跌越买"的深套，和波段目标冲突

因此本策略**纯右侧**：必须等 20 日热度抬升 + 动能确认后才入场。入场分两档：

```
首仓（信号日）：满足 R1 → 当日入场，仓位 = targetWeight × 首仓比例(默认 60%)
补仓（趋势内回调）：已持仓状态下，若出现以下条件之一 → 补足剩余 40%：
  a) 回调至 MA20 附近（Bias_20 ≤ +3%）且动能未降级
  b) 回调至 MA60 附近（Bias_20 ≤ 0%）且 MA60 斜率仍为正，板块逻辑未破坏
  c) 动能从"偏强向上"升级为"强势向上"（信号增强确认 → 直接补满）
```

**补仓的底线**：若补仓条件一直不出现，持有首仓 60% 直到出场信号触发，不做强制补仓。

> **含义**：信号日直接建首仓（不错过趋势），留 40% 等更好的价格（降成本），不追高打满。  
> **影响**：资金利用率不会闲置（首仓已进场），成本可能比满仓追高更低。

---

**信号强度分档**（用于 Portfolio 层仓位分配）：

| 入场时动能 | 信号强度 | 目标仓位系数 |
|------------|----------|-------------|
| 强势向上 | ⭐⭐⭐ 强 | 基准仓位 × 1.0（满仓） |
| 偏强向上 | ⭐⭐ 中 | 基准仓位 × 0.5（半仓试探） |

---

#### 【规则 2】清仓：IN → OUT — 过热清仓（清仓派，直接到 0%）

触发条件（**满足任一即触发**）：

```
① 正乖离过热
    即：Bias_20 > 0 且 Bias_20 >= 0.9 × Bias_20_History_Max

② 动能趋势破坏
    即：动能 ∈ { "弱势向下", "偏强向下" }

③ 阶段转坏
    即：阶段信号 ∈ { "衰退期" } 或 combo 操作建议 ∈ { "果断清仓", "清仓离场" }

④ 资金恐慌
    即：资金行为 = "恐慌出逃"
```

清仓后进入 COOLING，`exit_reason` 记录为 `"overheat"` 或 `"trend_break"` 或 `"stage_bad"` 或 `"panic"`。

---

#### 【规则 3】冷却：COOLING → 可再入的条件

##### 3a. 过热清仓后的冷却（`exit_reason = "overheat"`）

```
时间门槛：清仓后满 3 个交易日
条件门槛：Bias_20 <= 0.6 × Bias_20_History_Max（只对正乖离判断；若已转负，直接满足）
两个门槛同时满足 → 状态转为 OUT（可触发入场）
```

> **举例**：某 ETF 历史最大正乖离 +15%，在 +13.5%（≥ 0.9×15）清仓。  
> 第 4 个交易日（满足 3 天），若乖离回落到 +8%（≤ 0.6×15=9%），冷却解除，进入 OUT 等待入场信号。  
> 若第 4 天乖离仍在 +11%，继续冷却。

##### 3b. 趋势破坏清仓后的冷却（`exit_reason ≠ "overheat"`）

```
时间门槛：清仓后满 3 个交易日
条件门槛：动能恢复为 "偏强向上" 或 "强势向上" 
           且 Amount_Share_MA20 不再下降（MA20(t) >= MA20(t-1) 或当天抬升天数≥1）
两个门槛同时满足 → 状态转为 OUT（可触发入场）
```

> **解释**：趋势破坏后乖离可能已经为负值，不能用乖离回落去判断。  
> 此时需要等趋势重新建立——动能恢复 + 资金热度不再下滑。

---

### 4.3 状态机伪代码

```python
def compute_policy(signals_today, state_yesterday, symbol):
    """
    signals_today: dict { 动能, 资金行为, 阶段信号, 指标数据, 操作建议 }
    state_yesterday: dict { trend_state, exit_reason, cooldown_start, exit_date }
    返回: dict { trend_state, action, reason, signal_strength, cooldown_left }
    """
    current_state = state_yesterday["trend_state"]
    ind = signals_today["指标数据"]
    
    if current_state == "IN":
        # 先判断是否有补仓机会
        topup = check_topup(signals_today, state_yesterday)
        if topup:
            return {
                "trend_state": "IN",
                "action": "TOPUP",
                "reason": topup,
                "signal_strength": get_strength(signals_today),
                "cooldown_left": 0
            }
        # 判断是否触发清仓
        exit_type = check_exit(signals_today)
        if exit_type:
            return {
                "trend_state": "COOLING",
                "action": "EXIT",
                "reason": build_exit_reason(exit_type, signals_today),
                "signal_strength": None,
                "cooldown_left": 3,
                "exit_reason": exit_type
            }
        else:
            return {
                "trend_state": "IN",
                "action": "HOLD",
                "reason": "持有中，未触发出场条件",
                "signal_strength": get_strength(signals_today),
                "cooldown_left": 0
            }
    
    elif current_state == "COOLING":
        cooldown_days = trading_days_since(state_yesterday["exit_date"])
        if cooldown_days >= 3:
            if state_yesterday["exit_reason"] == "overheat":
                if check_overheat_cooled(ind):
                    return {"trend_state": "OUT", "action": "NONE", "reason": "冷却解除",
                            "cooldown_left": 0}
            else:
                if check_trend_recovery(signals_today):
                    return {"trend_state": "OUT", "action": "NONE", "reason": "趋势恢复，冷却解除",
                            "cooldown_left": 0}
        return {
            "trend_state": "COOLING",
            "action": "NONE",
            "reason": f"冷却中，还需{3 - cooldown_days}个交易日",
            "cooldown_left": 3 - cooldown_days
        }
    
    elif current_state == "OUT":
        if check_entry(signals_today):
            return {
                "trend_state": "IN",
                "action": "ENTER",
                "reason": f"20日热度连续3日抬升，动能：{signals_today['动能']}",
                "signal_strength": get_strength(signals_today),
                "cooldown_left": 0
            }
        else:
            return {"trend_state": "OUT", "action": "NONE", "reason": "不入场", "cooldown_left": 0}
```

---

### 4.4 状态持久化

每只 ETF 的状态落盘到独立文件：

```
data/policy/states/{symbol}.json
```

格式：
```json
{
  "symbol": "sh512480",
  "trend_state": "IN",
  "exit_reason": null,
  "exit_date": null,
  "entry_date": "2026-06-02",
  "topup_completed": false,
  "target_weight": 0.133,
  "cooldown_start": null,
  "as_of": "2026-06-05",
  "history": [
    {"date": "2026-05-20", "trend_state": "OUT", "action": "NONE"},
    {"date": "2026-05-21", "trend_state": "IN", "action": "ENTER", "entry_date": "2026-05-21", "target_weight": 0.10},
    {"date": "2026-05-23", "trend_state": "IN", "action": "TOPUP", "reason": "MA20回调补仓"},
    {"date": "2026-05-26", "trend_state": "COOLING","action": "EXIT", "exit_reason": "overheat"},
    {"date": "2026-05-29", "trend_state": "OUT", "action": "NONE"},
    {"date": "2026-06-02", "trend_state": "IN", "action": "ENTER", "entry_date": "2026-06-02", "target_weight": 0.133}
  ]
}
```

`history` 数组记录每个交易日的状态变更（只 append，用于审计和回放）。不超过 200 条，超出时剔除最早的。

---

### 4.5 初始状态

初始运行时所有 ETF 的 `trend_state = "OUT"`。  
系统从第一个可用交易日开始，逐日运行状态机，积累到当前日期的正确状态。  
这一步在 Python 脚本中以"回放模式"完成（从最早信号日期推到今天）。

---

## 五、Portfolio 层（仓位分配——前端）

### 5.1 基准仓位计算

```
activeTargets = { sym: signal_strength }
// 过滤出 action ∈ {ENTER, HOLD} 且 signal_strength ∈ {强, 中} 的标的

totalWeight = Σ strengthToWeight(signal_strength)
// 强 → 1.0, 中 → 0.5

baselineWeight = 1.0 / max(1, 进入标的总数)

对每个标的：
  targetWeight(sym) = baselineWeight × strengthToWeight(signal_strength) / totalWeight
```

**举例**（10万资金，3只满足条件）：
- A（强势向上，强）→ weight = 1.0 → targetWeight = 1/3 × 1.0 / (1.0+0.5+1.0) = 13.3%
- B（偏强向上，中）→ weight = 0.5 → targetWeight = 1/3 × 0.5 / 2.5 = 6.7%
- C（强势向上，强）→ weight = 1.0 → targetWeight = 1/3 × 1.0 / 2.5 = 13.3%
- 其余：targetWeight = 0%（清仓派，不在池中就为 0）

#### 价格匹配系数（性价比调节）

仓位不只看信号强弱，还看**当前价格相对均线的位置**——同样强信号，靠近均线买性价比更高，远离均线买追高风险更大。

```
价格系数 = 1.0 - (Bias_20 / Bias_20_History_Max) × 折扣因子(默认 0.5)

约束：价格系数 ∈ [0.5, 1.0]（最低不打对折以下，防止信号被完全抹掉）

释义（假设历史最大正乖离 = +15%）：
  Bias_20 = +3%（靠近MA20） → 系数 = 1.0 - (3/15)×0.5 = 0.90 → 几乎满配
  Bias_20 = +10%（远离MA20） → 系数 = 1.0 - (10/15)×0.5 = 0.67 → 缩减 1/3
  Bias_20 = +15%（极值边缘） → 系数 = 0.5（下限地板）

最终目标仓位 = 基准仓位 × 价格系数
```

> **含义**：同样"强势向上"，价格还在 MA20 附近的 ETF 权重更大（性价比高），已经涨了 10%+ 乖离的 ETF 权重自动缩减。  
> **影响**：自然实现"买便宜不买贵"，减少追高成本；信号转弱时，高价仓位的缩减也等于提前减仓。

#### 仓位计算完整公式

```
step1: baselineWeight = 1.0 / nActive          // 等权基准
step2: signalWeight = baselineWeight × strengthToWeight(strength) / ΣstrengthToWeight
       // 强=1.0, 中=0.5
step3: priceFactor = clamp(1.0 - (Bias20/HistoryMax)*0.5, 0.5, 1.0)
       // 仅对 Bias20 > 0 应用；Bias20 ≤ 0 时 priceFactor = 1.0
step4: targetWeight = signalWeight × priceFactor
       // 最终目标仓位
```

### 5.2 板块上限约束

```
maxCategoryPct = 30% （可在 simConfig 中调整）
若某板块仓位超过上限，按等比例缩减该板块内所有标的的目标仓位，超出部分归入现金。
```

### 5.3 现金约束

```
buyNotional = min(targetWeight - currentWeight) × equity, cash
若 buyNotional <= 0，该笔不生成买入建议。
```

### 5.4 调仓阈值

```
|targetWeight - currentWeight| < rebalanceBandPct / 100 → 不触发建议（避免微调抖动）
默认 band = 5%
```

---

## 六、后端实现清单

### 6.1 新增文件

| 文件 | 用途 |
|------|------|
| `treasolo/m1_policy.py` | Policy 层状态机 + 批量计算 + 状态持久化 |
| `data/policy/states/` | 目录，存放每只 ETF 的状态文件 |

### 6.2 修改文件

| 文件 | 改动 |
|------|------|
| `treasolo/m1_sector_lifecycle.py` | `analyze_sector()` 追加 `Amount_Share_MA20`、`Amount_Share_Change20`、`Amount_Share_DaysUp` 三个字段到 `指标数据` |
| `server.js` | 新增 `GET /api/m1/policy` 端点，返回所有 ETF 的 policy 输出（trend_state/action/reason/signal_strength 等） |
| `server.js` | `GET /api/m1/data/overview` 中将 policy 数据嵌入返回（不走额外请求） |

### 6.3 `treasolo/m1_policy.py` 核心函数

```python
# 1. 状态机核心
def compute_policy(signals_today: dict, state_yesterday: dict) -> dict

# 2. 入场判定
def check_entry(signals: dict) -> bool

# 3. 补仓判定
def check_topup(signals: dict, state: dict) -> Optional[str]  # 返回补仓原因 或 None

# 4. 出场判定
def check_exit(signals: dict) -> Optional[str]  # 返回 exit_reason 或 None

# 5. 过热冷却判定
def check_overheat_cooled(ind: dict) -> bool

# 6. 趋势恢复判定
def check_trend_recovery(signals: dict) -> bool

# 7. 价格匹配系数
def calc_price_factor(bias20: float, history_max: float, discount: float = 0.5) -> float

# 8. 批量计算（命令行入口）
def run_policy(day: str) -> dict

# 9. 回放模式（初始化用）
def replay_policy(start_date: str, end_date: str) -> dict
```

### 6.4 API 端点设计

**`GET /api/m1/policy`**

返回：
```json
{
  "day": "2026-06-08",
  "policies": {
    "sh512480": {
      "trend_state": "IN",
      "action": "HOLD",
      "reason": "持有中，未触发出场条件",
      "signal_strength": "强",
      "cooldown_left": 0,
      "entry_date": "2026-06-02",
      "topup_completed": false,
      "target_weight": 0.133,
      "price_factor": 0.85,
      "asOf": "2026-06-08T15:05:00"
    },
    "sh512400": {
      "trend_state": "COOLING",
      "action": "NONE",
      "reason": "冷却中，还需2个交易日",
      "signal_strength": null,
      "cooldown_left": 2,
      "exit_reason": "trend_break",
      "asOf": "2026-06-08T15:05:00"
    },
    "sh516010": {
      "trend_state": "OUT",
      "action": "NONE",
      "reason": "资金热度未确认，不入场",
      "signal_strength": null,
      "cooldown_left": 0,
      "asOf": "2026-06-08T15:05:00"
    }
  }
}
```

---

## 七、前端改动清单

### 7.1 总览区（现有：模拟账户设置旁）

新增 **策略参数卡**：

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 热度窗口 | 20 | Amount_Share 的 MA 周期 |
| 热度抬升天数 | 3 | 连续抬升天数阈值 |
| 过热阈值比 | 0.9 | Bias_20 ≥ 0.9×Max 清仓 |
| 冷却天数(最小) | 3 | COOLING 最短天数 |
| 乖离回落比 | 0.6 | 过热冷却时 Bias 回落比例 |
| 入场动能门槛 | 偏强向上 | 半仓/满仓的动能分界 |
| 首仓比例 | 0.6 | 入场时目标仓位的执行比例（剩余等回调补） |
| 价格折扣因子 | 0.5 | 乖离越大仓位缩减越多（0=不缩减，1=最大缩减） |
| 补仓回调线 MA20 | 3% | Bias ≤ 此值触发回调补仓 |
| 补仓回调线 MA60 | 0% | Bias ≤ 此值触发更深回调补仓 |

> 改为 `policyConfig`，风格与现有 `simConfig` 一致，存 localStorage。

### 7.2 建议表格区（现有：关注 ETF 全列表）

新增 3 列：

| 新增列 | 取值 | 数据来源 |
|--------|------|----------|
| 趋势状态 | `IN / OUT / COOLING(剩2天)` | `policyData[sym].trend_state` + `cooldown_left` |
| 触发类型 | `入场确认 / 趋势破坏 / 过热清仓 / 冷却中` | `policyData[sym].action` + `exit_reason` 映射 |
| 信号强度 | `强 / 中 / —` | `policyData[sym].signal_strength` |

原"池子"列改为趋势状态列（或并列显示）。

### 7.3 建议生成逻辑改造

现有 [ui_m1.js:tradeSuggestions](file:///Users/una5577/Documents/trae_projects/a-stock-monitor/public/ui_m1.js#L900-L956) 改为基于 `policyData`：

```javascript
const tradeSuggestions = computed(() => {
  // 输入：policyData[sym].action / signal_strength
  // 输出：建议列表（买入金额/卖出金额/原因）
  
  const activeSet = [];
  Object.keys(policyData).forEach(sym => {
    const p = policyData[sym];
    if (p.action === 'ENTER' || p.action === 'HOLD') {
      activeSet.push({ sym, strength: p.signal_strength });
    }
  });
  
  // 仓位分层（强=1.0, 中=0.5）
  // 板块上限约束 + 现金约束
  // 调仓阈值过滤（< 5% 不触发）
  // ...
});
```

### 7.4 确认弹窗增强

现有一个 trade 确认 dialog，追加只读信息行：

```
本次动作：入场建仓（首仓 60%）/ 补仓（MA20回调 / 信号升级）/ 清仓离场 / 冷却中(剩2天)
触发原因：20日热度连续3日抬升，动能：强势向上 / 回调至MA20附近，Bias=+2.5%
执行后仓位：0% → 8.0%（首仓，目标 13.3%）/ 8.0% → 13.3%（补仓完成）
价格匹配：乖离率 +2.5%，历史极值 +15%，价格系数 0.92 → 仓位无缩减
本次是否触发风控：无 / 板块科技超配(34%)→自动缩减至30%
```

### 7.5 账户明细区新增

在"持仓表格"和"交易记录"旁增加：

**净值与回撤卡片**：
- 模拟账户净值（equity）
- 最大回撤（从历史最高 equity 算）
- 单标的最大回撤排行（用于止损监控）

**当前已触发止损的标的**（高亮提示）。

---

## 八、回测与策略进化

### 8.1 回测脚本

新建 `scripts/backtest_swing.py`：

```
输入：
  - etf_symbols（9只）
  - date_range（回测时间范围，默认最近1年）
  - policy_params（入场/出场/冷却参数）
  - initial_cash（初始资金）

处理：
  1. 读取每日 ETF 日线数据
  2. 逐日计算 Feature → Signal → Policy
  3. 模拟交易执行（买入/卖出/持有）
  4. 记录每日净值

输出：
  - data/backtests/{run_id}/equity_curve.json  净值曲线
  - data/backtests/{run_id}/trades.json        逐笔交易
  - data/backtests/{run_id}/metrics.json       绩效指标
```

### 8.2 绩效指标

| 指标 | 计算方式 | 含义 |
|------|----------|------|
| 总收益率 | (最终净值 - 初始资金) / 初始资金 | 绝对收益 |
| 年化收益率 | CAGR | 时间归一化 |
| 最大回撤 | 从最高点到最低点的最大跌幅 | 风险 |
| 夏普比率 | (年化收益 - 无风险利率) / 年化波动率 | 风险调整收益 |
| 胜率 | 盈利交易 / 总交易 | 选股能力 |
| 盈亏比 | 平均盈利 / 平均亏损 | 风险控制 |
| 信号翻转率 | 连续 ENTER→EXIT 隔日比例 | 策略稳定性 |
| 持仓天数分布 | 平均/中位数 | 波段特征 |

### 8.3 参数扫描

对关键参数做网格扫描，找最优组合：

| 参数 | 扫描范围 | 步长 |
|------|----------|------|
| 热度抬升天数 | 2 / 3 / 5 | — |
| 过热阈值比 | 0.85 / 0.90 / 0.95 | 0.05 |
| 冷却最小天数 | 2 / 3 / 5 | — |
| 乖离回落比 | 0.5 / 0.6 / 0.7 | 0.1 |
| 入场动能门槛 | 偏强向上 / 强势向上 | — |

扫描脚本：`scripts/scan_params.py`，输出参数 vs 绩效的对比表。

### 8.4 滚动学习（每月自动回测 + 推荐调参）

```
定时任务（每月第 1 个交易日盘后）：
1. 用最近 12 个月日线跑回测（当前参数）
2. 若最大回撤恶化或夏普下降 > 20%：
   自动跑参数扫描
   输出对比报告（当前 vs 最优）
   保存到 data/policy/reports/{date}_param_scan.json
3. 人工确认后更新参数文件
```

参数文件：`data/policy/params.json`
```json
{
  "heat_window": 20,
  "heat_up_days": 3,
  "overheat_ratio": 0.9,
  "cooldown_min_days": 3,
  "bias_reset_ratio": 0.6,
  "entry_momentum": "偏强向上",
  "first_entry_ratio": 0.6,
  "price_discount_factor": 0.5,
  "topup_bias_ma20": 3.0,
  "topup_bias_ma60": 0.0,
  "stop_warn_pct": 8.0,
  "stop_exec_pct": 12.0,
  "last_updated": "2026-06-08"
}
```

---

## 九、数据流汇总

```
每日盘后流程：

1. [Python] m1_warmup.py    → data/warmup/warmup-60.json（60日预热）
2. [Python] m1_lifecycle.py  → data/lifecycle/lifecycle.json（日频信号快照）
3. [Python] m1_policy.py     → data/policy/states/{symbol}.json（状态机更新）
                            → data/policy/policy_{date}.json（供API读取）
4. [Node]  server.js         → GET /api/m1/policy（供前端消费）
                            → GET /api/m1/data/overview 合并返回
5. [前端]  ui_m1.js          → 读取 lifecycle（展示诊断） + policy（交易决策）
                            → tradeSuggestions computed（仓位分配 + 确认弹窗）
                            → 用户确认 → applySimTradeExact → saveSimLocal
```

---

## 十、规则编号速查表

| 编号 | 名称 | 触发条件 | 动作 |
|------|------|----------|------|
| R1 | 入场 | 热度连升3天 + Change20>0 + 动能偏强↑ + 不过热 | ENTER（首仓=目标×60%） |
| R1a | 补仓-MA20回调 | 已持仓 + Bias≤3% + 动能未降级 | 补至目标×100% |
| R1b | 补仓-MA60回调 | 已持仓 + Bias≤0% + MA60斜率>0 | 补至目标×100% |
| R1c | 补仓-信号升级 | 已持仓 + 动能从"偏强"升至"强势" | 补至目标×100% |
| R2a | 过热清仓 | Bias≥0.9×Max | EXIT → COOLING(overheat) |
| R2b | 趋势破坏清仓 | 动能弱势/偏强向下 或 衰退/背离阶段 | EXIT → COOLING(trend_break) |
| R2c | 恐慌清仓 | 资金行为=恐慌出逃 | EXIT → COOLING(panic) |
| R3a | 过热冷却解除 | ≥3天 + Bias≤0.6×Max | COOLING → OUT |
| R3b | 趋势冷却解除 | ≥3天 + 动能恢复 + MA20不再降 | COOLING → OUT |
| R4 | 买入仓位分层 | ENTER时动能=强→满仓；中→半仓 | 目标仓位系数 |
| R5 | 板块上限 | 板块仓位>maxCategoryPct → 等比例缩减 | 截断 |
| P1 | 价格匹配 | Bias/HistoryMax 越大仓位系数越低 | 目标仓位×价格系数∈[0.5,1.0] |
| S1 | 止损-预警 | 单标从最高浮盈回撤≥8% | 减半仓 |
| S2 | 止损-执行 | 单标从最高浮盈回撤≥12% | 清仓 |

---

*文档生成日期：2026-06-08*
*适用范围：a-stock-monitor 项目交易助手模块*
