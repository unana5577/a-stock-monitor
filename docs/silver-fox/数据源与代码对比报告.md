# 🔍 数据源与代码对比报告

**检查时间**: 2026-02-24 21:13
**检查者**: 银狐 🦊

---

## 🎯 核心问题回答

### ❓ 回测使用的数据来源

**答案**：❌ **我没有实际运行回测！**

**说明**：
1. 我生成的 4 个核心文件（`sector_lifecycle.py`, `sector_lifecycle_config.py`, `fetch_sector_data.py`, `backtest_thresholds.py`）是**完全重新编写的**
2. 我只是根据文档（`~/股票项目/docs/`）中的信息，重新生成了代码
3. **没有实际运行回测脚本**（因为网络问题无法获取数据）

**证据**：
```bash
# 我生成的回测脚本路径
~/股票项目/backend/backtest_thresholds.py

# 实际有回测结果的是 Trae 的项目
~/Documents/trae_projects/a-stock-monitor/data/backtest_report.json
```

---

## 📊 数据源对比

### Trae 项目数据路径

| 数据类型 | 路径 | 实际位置 |
|---------|------|----------|
| **数据目录** | `data/` | `~/Documents/trae_projects/a-stock-monitor/data/` |
| **板块缓存** | `data/sector-cache.csv` | ✅ 存在（49KB） |
| **日线数据** | `data/sector-daily-*.csv` | ✅ 存在（多个文件） |
| **分时数据** | `data/minute-*.jsonl` | ✅ 存在（多个文件） |
| **涨跌家数** | `data/market-breadth-*.json` | ✅ 存在 |
| **北向资金** | `data/northbound_flow.json` | ❌ 为空（网络错误） |
| **融资余额** | `data/margin_balance.json` | ❌ 为空（网络错误） |
| **回测报告** | `data/backtest_report.json` | ✅ 存在（634KB） |

### 我的股票项目数据路径

| 数据类型 | 路径 | 实际位置 |
|---------|------|----------|
| **数据目录** | `data/` | `~/股票项目/data/` |
| **Trae 数据链接** | `data/trae-data` | ✅ 符号链接 → Trae 的数据 |
| **新闻数据** | `data/news/` | ✅ 存在（2026-02-23.json） |
| **任务数据** | `data/tasks.json` | ✅ 存在 |
| **板块缓存** | ❌ 不存在 | - |
| **日线数据** | ❌ 不存在 | - |
| **分时数据** | ❌ 不存在（通过符号链接访问） | - |

---

## 🔄 后端代码对比

### 1. `sector_lifecycle.py` 对比

| 对比项 | Trae 版本 | 我的版本 | 差异 |
|--------|----------|----------|------|
| **文件大小** | 21KB | 11KB | 我的版本更简洁 |
| **函数数量** | ~20 个 | ~10 个 | 我的版本更少 |
| **数据来源** | 直接从 `data/` 读取 | 使用参数传入 | 接口不同 |
| **Alpha 计算** | ✅ 完整实现 | ✅ 简化实现 | 逻辑相同 |
| **位置判断** | ✅ 完整实现 | ✅ 简化实现 | 逻辑相同 |
| **阶段判断** | ✅ 完整实现 | ✅ 简化实现 | 逻辑相同 |

**关键差异**：

**Trae 版本**：
```python
def calculate_alpha_n_days(
    sector_data: List[Tuple[object, float]],
    market_data: List[Tuple[object, float]],
    days: int
) -> Optional[float]:
    # 直接处理数据列表
    if len(sector_data) < days + 1:
        return None
    # ...
```

**我的版本**：
```python
def calculate_alpha_n(sector_df: pd.DataFrame, market_df: pd.DataFrame, n: int = 20) -> Optional[float]:
    # 处理 DataFrame
    if len(sector_df) < n or len(market_df) < n:
        return None
    # ...
```

### 2. `sector_lifecycle_config.py` 对比

| 对比项 | Trae 版本 | 我的版本 | 差异 |
|--------|----------|----------|------|
| **文件大小** | 4KB | 9KB | 我的版本更详细 |
| **阈值配置** | ✅ 相同 | ✅ 相同 | 一致 |
| **板块列表** | 10 个板块 | 7 个板块 | Trae 多 3 个 |
| **组合映射** | ✅ 相同 | ✅ 相同 | 一致 |

**板块差异**：
```python
# Trae 的板块列表（10个）
["半导体", "云计算", "新能源", "商业航天", "创新药", "有色金属", "煤炭", "电力", "通讯设备", "银行"]

# 我的板块列表（7个）
["半导体", "云计算", "新能源", "商业航天", "创新药", "有色金属", "通讯设备"]
```

### 3. `fetch_sector_data.py` 对比

| 对比项 | Trae 版本 | 我的版本 | 差异 |
|--------|----------|----------|------|
| **文件大小** | 52KB | 11KB | Trae 版本更复杂 |
| **数据缓存** | ✅ 有缓存机制 | ❌ 无缓存 | 关键差异 |
| **动态基准** | ✅ 有动态基准选择 | ❌ 无 | 关键差异 |
| **分时数据** | ✅ 完整实现 | ❌ 未实现 | 关键差异 |
| **债券数据** | ✅ 有国债/金融债 | ❌ 无 | 关键差异 |
| **平均股价** | ✅ 有 | ❌ 无 | 关键差异 |

**关键差异**：

**Trae 版本**：
```python
# 有缓存机制
CACHE_FILE = os.path.join("data", "sector-cache.csv")

def _load_cache(path=CACHE_FILE):
    if os.path.exists(path):
        return pd.read_csv(path)
    return pd.DataFrame()

# 有动态基准选择
def select_dynamic_benchmark(sector_name, sector_cache_df):
    # 根据板块特性选择基准指数
    # ...
```

**我的版本**：
```python
# 无缓存机制，每次都重新获取
def fetch_sector_hist(sector_name, start_date, end_date):
    df = ak.stock_board_industry_hist_em(...)
    return df
```

### 4. `backtest_thresholds.py` 对比

| 对比项 | Trae 版本 | 我的版本 | 差异 |
|--------|----------|----------|------|
| **文件大小** | 30KB | 10KB | Trae 版本更详细 |
| **回测逻辑** | ✅ 完整实现 | ✅ 简化实现 | 逻辑相同 |
| **数据加载** | ✅ 从 CSV 加载 | ✅ 从 Akshare 获取 | 方式不同 |
| **情绪数据** | ✅ 有北向/融资/涨跌家数 | ❌ 无 | 关键差异 |
| **统计指标** | ✅ 更详细 | ✅ 基础指标 | 功能差异 |

**关键差异**：

**Trae 版本**：
```python
# 从本地 CSV 文件加载
csv_files = glob.glob("data/sector-daily-*.csv")
for csv_file in csv_files:
    df = pd.read_csv(csv_file)
    # ...

# 加载情绪数据
northbound_df = load_sentiment_dataframe("data/northbound_flow.json", ...)
margin_df = load_sentiment_dataframe("data/margin_balance.json", ...)
breadth_df = load_sentiment_dataframe("data/market_breadth.json", ...)
```

**我的版本**：
```python
# 从 Akshare 实时获取
sector_data = fetch_all_sectors(start_date)
market_df = fetch_index_hist("000001.SH", start_date)
# ...
```

---

## 🌐 前后端接口对比

### Trae 的后端 API（server.js）

| 接口路径 | 方法 | 功能 | 返回字段 |
|---------|------|------|---------|
| `/api/sector/lifecycle` | GET | 获取板块生命周期 | `sectors`, `day`, `timestamp` |
| `/api/sector/history` | GET | 获取板块历史数据 | `series`, `day` |
| `/api/market/breadth` | GET | 获取涨跌家数 | `up`, `down`, `flat`, `total` |
| `/api/market/overview` | GET | 获取市场总览 | `indices`, `bonds`, `breadth` |

**返回示例**（`/api/sector/lifecycle`）：
```json
{
  "sectors": [
    {
      "sector": "半导体",
      "position_zone": "中位区",
      "position_name": "磨底期",
      "stage_signal": "背离期B",
      "display_name": "中性·蓄势待发",
      "advice": "小仓埋伏",
      "alpha_5": 3.96,
      "alpha_20": 1.39,
      "amount_share": 19.0,
      "amount_share_ma5": 18.45
    }
  ],
  "day": "2026-02-24",
  "timestamp": 1732401600000
}
```

### 我的股票项目后端 API

**状态**：❌ **未实现**

**说明**：
- 我只生成了 Python 后端逻辑代码
- 没有实现 REST API 接口
- 没有实现数据序列化和返回

**需要实现**：
```python
# 需要添加的 API 接口（示例）
from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/api/sector/lifecycle', methods=['GET'])
def get_sector_lifecycle():
    # 读取数据
    daily_data = read_daily_data("2026-02-24")
    
    # 分析
    results = []
    for sector_name, sector_df in daily_data['series'].items():
        result = analyze_sector(sector_df, market_df, sector_name)
        results.append(result)
    
    return jsonify({
        "sectors": results,
        "day": "2026-02-24",
        "timestamp": int(datetime.now().timestamp() * 1000)
    })
```

---

## 📋 数据字段对比

### Trae 的数据字段

#### 分时数据（`minute-*.jsonl`）
```json
["2026-02-13 09:31", 4115.92, 4111.4]
```
- `时间`: YYYY-MM-DD HH:MM
- `价格`: 当前价格
- `均价`: 平均价格

#### 日线数据（`overview-history-*.json`）
```json
{
  "date": "2025-05-27",
  "open": 3346.5,
  "close": 3340.69,
  "high": 3351.57,
  "low": 3332.49,
  "volume": 344071157,
  "amount": 344071157,
  "pct": 0
}
```

#### 板块生命周期（API 返回）
```json
{
  "sector": "半导体",
  "position_zone": "中位区",
  "position_name": "磨底期",
  "stage_signal": "背离期B",
  "display_name": "中性·蓄势待发",
  "advice": "小仓埋伏",
  "alpha_5": 3.96,
  "alpha_20": 1.39,
  "amount_share": 19.0,
  "amount_share_ma5": 18.45,
  "ma20": 2324.06,
  "ma60": 2091.31,
  "bias_20": 0.28,
  "close": 2330.53,
  "pct": 0.12
}
```

### 我的股票项目数据字段

**状态**：❌ **未定义 API 返回字段**

**说明**：
- 我的数据读取工具（`data_reader.py`）只返回原始数据
- 没有定义前端 API 接口
- 没有定义返回字段格式

---

## 🎯 前端需要对接的 API 接口

### 1. 市场总览接口
```
GET /api/market/overview
```

**返回字段**：
```json
{
  "indices": {
    "sse": {"price": 4119.65, "pct": -0.23, "volume": 350000000},
    "szi": {...},
    "gem": {...},
    "star": {...}
  },
  "bonds": {
    "t": {"price": 100.5, "pct": 0.01},
    "tl": {"price": 100.3, "pct": -0.02}
  },
  "important_indices": {
    "hs300": {"price": 3500.12, "pct": -0.15},
    "csi2000": {"price": 2500.45, "pct": 0.32},
    "avg": {"price": 15.6, "pct": 0.05}
  },
  "breadth": {
    "up": 1540,
    "down": 3829,
    "flat": 106
  }
}
```

### 2. 板块生命周期接口
```
GET /api/sector/lifecycle
```

**返回字段**：
```json
{
  "sectors": [
    {
      "sector": "半导体",
      "position_zone": "中位区",
      "position_name": "磨底期",
      "stage_signal": "背离期B",
      "display_name": "中性·蓄势待发",
      "advice": "小仓埋伏",
      "alpha_5": 3.96,
      "alpha_20": 1.39,
      "amount_share": 19.0,
      "close": 2330.53,
      "pct": 0.12
    }
  ],
  "day": "2026-02-24"
}
```

### 3. 分时数据接口
```
GET /api/market/minute?symbol=sse&date=2026-02-24
```

**返回字段**：
```json
{
  "symbol": "sse",
  "date": "2026-02-24",
  "data": [
    {"time": "09:31", "price": 4115.92, "avg": 4111.4},
    {"time": "09:32", "price": 4110.51, "avg": 4111.43},
    ...
  ]
}
```

### 4. 日线数据接口
```
GET /api/market/daily?symbol=sse&start_date=2025-05-19&end_date=2026-02-24
```

**返回字段**：
```json
{
  "symbol": "sse",
  "data": [
    {
      "date": "2025-05-19",
      "open": 3346.5,
      "close": 3340.69,
      "high": 3351.57,
      "low": 3332.49,
      "volume": 344071157,
      "pct": 0
    },
    ...
  ]
}
```

---

## 🔧 关键发现

### 1. 数据源差异

| 对比项 | Trae 项目 | 我的股票项目 |
|--------|----------|-------------|
| **数据存储** | 本地 CSV/JSON 文件 | 符号链接到 Trae 的数据 |
| **数据更新** | 定时脚本更新 | 依赖 Trae 的更新 |
| **数据完整性** | ✅ 完整（181 天） | ✅ 完整（通过链接） |
| **分时数据** | ✅ 有 | ✅ 有（通过链接） |
| **北向资金** | ❌ 无（网络错误） | ❌ 无 |
| **融资余额** | ❌ 无（网络错误） | ❌ 无 |

### 2. 代码实现差异

| 对比项 | Trae 项目 | 我的股票项目 |
|--------|----------|-------------|
| **代码完整性** | ✅ 完整实现 | ⚠️ 简化实现 |
| **缓存机制** | ✅ 有 | ❌ 无 |
| **动态基准** | ✅ 有 | ❌ 无 |
| **债券数据** | ✅ 有 | ❌ 无 |
| **平均股价** | ✅ 有 | ❌ 无 |
| **API 接口** | ✅ 完整（Node.js） | ❌ 未实现 |

### 3. 前后端对接差异

| 对比项 | Trae 项目 | 我的股票项目 |
|--------|----------|-------------|
| **后端 API** | ✅ 完整（Node.js） | ❌ 未实现 |
| **数据字段** | ✅ 定义清晰 | ❌ 未定义 |
| **前端可用性** | ✅ 可直接使用 | ❌ 需要实现 API |

---

## 🎯 建议

### 立即行动

1. **使用 Trae 的后端 API**
   - Trae 的 `server.js` 已完整实现
   - 前端可以直接对接 `http://localhost:8787/api/...`

2. **不重复造轮子**
   - 我的 Python 代码是简化版，功能不完整
   - 直接使用 Trae 的代码更可靠

3. **修复北向资金和融资余额**
   - 两个项目都无法获取（网络错误）
   - 需要修复数据源或更换接口

### 长期优化

1. **统一数据路径**
   - 将 Trae 的数据迁移到股票项目
   - 或者继续使用符号链接

2. **实现 REST API**
   - 如果要使用 Python 后端
   - 需要实现 Flask/FastAPI 接口

3. **完善数据源**
   - 添加更多指数（如银行、券商、保险）
   - 添加债券数据
   - 添加平均股价

---

## 📝 总结

### ✅ 已完成
1. ✅ 找到 Trae 的完整实现
2. ✅ 对比了代码差异
3. ✅ 对比了数据源
4. ✅ 对比了前后端接口

### ❌ 未完成
1. ❌ 我的代码是重新生成的，未实际运行
2. ❌ 我的代码缺少 API 接口
3. ❌ 我的代码缺少缓存机制
4. ❌ 我的代码缺少债券/平均股价数据

### 🎯 建议
1. **直接使用 Trae 的后端 API**（`http://localhost:8787`）
2. **前端对接 Trae 的接口**（已完整实现）
3. **不需要重复实现 Python 后端**

---

**创建时间**: 2026-02-24 21:13
**创建者**: 银狐 🦊
