好的，我将基于 v2.1 需求文档，整合上述三条补充建议，生成一份更新后的 **v2.2 需求文档**。新增或修改的部分将用 **【新增】** 或 **【修订】** 标注，以便你快速识别。

---

# 板块生命周期判断系统 - 需求文档 (v2.2)
> **更新说明**:  
> - 明确数据可得性约束（2.3）  
> - 增加数据管理规范（2.4）  
> - 阈值体系由绝对值调整为分位数/标准化（贯穿第5、7章）  
> - 明确动态基准的重算频率和回测冻结规则（4.3）  

---

## 一、概述

### 1.1 目标

为A股量化软件（或自用分析工具）重新设计板块生命周期判断系统，通过**大盘情绪 + 动态基准 + 资金热度（动能+资金）** 三大核心模块，结合新闻因子作为修正，帮助用户识别板块状态，并给出3-5天的操作建议。系统将先从行业ETF入手，验证逻辑后再扩展至真实板块指数和个股。

### 1.2 核心逻辑

```
市场情绪过滤 → 趋势判断 → 资金行为 → 风险等级 → 操作建议
   (顶层)      (定方向)    (看量价)    (控仓位)    (最终决策)
新闻因子作为修正系数，调整最终建议的置信度
```

**关键原则**：
- **操作建议**主要由「动能+资金行为」决定
- **板块位置**作为参考信息，帮助理解整体状态
- **大盘情绪**作为顶层过滤器，在极端市场下修正或过滤信号
- **新闻因子**后期加入，用于修正建议强度

### 1.3 适用范围

| 场景 | 说明 |
|------|------|
| 行业ETF分析 | **第一阶段目标**，跑通逻辑 |
| 板块指数分析 | 第二阶段扩展，需调整数据源和阈值 |
| 个股分析 | 未来复用，需单独调整阈值 |

---

## 二、数据准备（阶段0）

### 2.1 基础数据源

| 数据类型 | 内容 | 说明 |
|----------|------|------|
| 行业ETF | 7个：半导体、云计算、新能源、商业航天、创新药、有色金属、通讯设备 | 使用ETF代码，获取日线数据 |
| 基准指数 | 4个：上证指数、深证成指、创业板指、科创50 | 用于动态基准选择 |
| 全市场成交额 | 全A股每日成交总额 | 计算资金热度（板块成交占比） |
| 大盘情绪数据 | 全市场涨跌家数、连板高度等 | 阶段7加入 |
| 北向资金 | 北向资金日度净流入 | 阶段9加入 |
| 融资余额 | 两市融资余额变化 | 阶段9加入 |
| 新闻数据 | 板块相关新闻标题及发布时间 | 阶段9加入 |

**数据范围**：
- 起始日期：2025-05-19
- 默认请求：1年（365天），逐步扩展
- 字段：date, open, high, low, close, volume, amount（成交额）

### 2.2 数据存储
- 初期使用 CSV 或 Parquet 文件存储
- 后期可迁移至数据库（如 SQLite、MySQL）

### 2.3 数据可得性约束 【新增】
为确保开发过程中不因数据不可得而返工，明确各阶段可用的核心字段及代理方案：

| 数据类别 | 当前阶段（ETF）可用字段 | 未来阶段（板块/个股）可用字段 | 备注 |
|----------|------------------------|-------------------------------|------|
| 价格数据 | open, high, low, close, volume | 同左 | 均可稳定获取 |
| 成交额 | amount（ETF成交额） | 板块指数成交额（需确认数据源） | ETF成交额可作为板块资金关注的代理 |
| 全市场成交额 | 全A股成交总额 | 同左 | 用于计算 Amount_Share |
| 涨跌家数 | ❌ | ✅（阶段7引入） | 需额外获取全市场个股数据 |
| 连板高度 | ❌ | ✅（阶段7引入） | 需统计涨停板数据 |
| 北向资金 | ❌ | ✅（阶段9引入） | 需外部数据接口 |
| 融资余额 | ❌ | ✅（阶段9引入） | 需外部数据接口 |
| 新闻情感 | ❌ | ✅（阶段9引入） | 需爬虫或API |

### 2.4 数据管理规范 【新增】
为保证数据长期维护的规范性，制定以下最小规范：

#### 2.4.1 文件命名格式
`{symbol}_{start_date}_{end_date}_{source}.parquet`  
示例：`半导体ETF_20250519_20260305_tushare.parquet`

#### 2.4.2 元数据要求
每个数据文件同级存放一个同名 `.json` 文件，记录：
- `source`：数据源（如 tushare, wind, 自定义）
- `fetch_time`：拉取时间（ISO 格式）
- `fields`：字段说明（如 {"open": "开盘价", "close": "收盘价"}）
- `adjust_type`：复权类型（前复权/后复权/不复权）
- `calendar`：交易日历版本（如 "SSE"）

#### 2.4.3 更新策略
- **日常更新**：每日收盘后增量更新（追加当日数据）。
- **非交易日处理**：回测时直接跳过非交易日，不填充数据；实时计算时若当天非交易日，则使用最近一个交易日数据。
- **数据对齐**：所有时间序列按交易日对齐，缺失值处理规则为向前填充（若需连续指标）。向前填充仅用于“指标计算”的中间序列对齐，不用于价格本身与收益回测口径，避免引入虚假价格/收益。

---

## 三、指标定义

### 3.1 基础指标

| 后台指标 | 前台显示 | 公式 | 含义 |
|----------|----------|------|------|
| Alpha_5 | 5日超额收益 | 板块5日涨幅 - 基准5日涨幅 | 短期相对强弱 |
| Alpha_20 | 20日超额收益 | 板块20日涨幅 - 基准20日涨幅 | 中期相对强弱 |
| Amount_Share | 当日资金关注度 | 板块成交额 / 全市场成交额 | 资金关注程度 |
| Amount_Share_MA5 | 5日资金热度 | 近5日 Amount_Share 均值 | 资金关注趋势 |
| Amount_Share_Change | 资金热度变化 | Amount_Share / Amount_Share_MA5 - 1 | 相对5日均值的增长 |

### 3.2 价格指标

| 后台指标 | 前台显示 | 公式 | 含义 |
|----------|----------|------|------|
| Close | 收盘价 | - | 当日收盘价 |
| Pct | 今日涨跌 | (今收 - 昨收) / 昨收 × 100% | 当日涨跌幅 |
| MA5 | 5日均线 | 近5日收盘价均值 | 短期趋势线 |
| MA20 | 20日均线 | 近20日收盘价均值 | 中期趋势线 |
| MA60 | 60日均线 | 近60日收盘价均值 | 长期趋势线 |

### 3.3 趋势指标

| 后台指标 | 前台显示 | 公式 | 含义 |
|----------|----------|------|------|
| MA5_Slope | 5日均线斜率 | (MA5今日 - MA5_5日前) / 5 | 短期趋势方向 |
| MA60_Slope | 60日均线斜率 | (MA60今日 - MA60_5日前) / 5 | 长期趋势方向 |
| Bias_20 | 偏离程度 | (收盘价 - MA20) / MA20 × 100% | 偏离20日均线的程度 |
| Bias_20_History_Max | 历史最大偏离 | 历史所有 Bias_20 的最大值 | 历史正乖离极值参考 |
| Bias_20_History_Min | 历史最小偏离 | 历史所有 Bias_20 的最小值 | 历史负乖离极值参考 |

### 3.4 辅助指标

| 后台指标 | 前台显示 | 含义 |
|----------|----------|------|
| RS_Change_20 | 强度变化 | Alpha_20 的变化趋势（暂缓实现） |
| Amount_Share_High_20 | 20日资金高点 | 近20日资金关注度最高值 |

---

## 四、动态基准选择（阶段1）

### 4.1 选择逻辑

| 步骤 | 说明 |
|------|------|
| 1 | 获取板块（ETF）过去60日收盘价序列 |
| 2 | 获取4个基准指数（上证、深证、创业板、科创板）60日收盘价序列 |
| 3 | 计算板块与每个指数的皮尔逊相关系数 |
| 4 | 选择相关系数最高的指数作为该板块的基准 |

### 4.2 输出字段

| 字段 | 示例 |
|------|------|
| 基准指数 | 创业板 |
| 相关性 | 0.85 |

### 4.3 后续使用及规则 【修订】

所有 Alpha 计算都基于动态选择的基准。**重算频率与冻结规则**如下：

- **重算频率**：每月第一个交易日重新计算一次基准指数（基于过去60日数据）。期间保持不变，以保证稳定性。
- **回测冻结规则**：在回测时，对于任意交易日 T，使用的基准必须是基于 **T-60 日至 T-1 日**的数据计算得出的，即只能用历史已知信息，严禁引入未来数据。
- **实盘应用**：当日实时计算时，使用最近一次重算的基准（即本月基准），不实时切换。

---

## 五、三层判断逻辑（阶段2，初始阈值版）

### 5.1 第一层：板块位置（参考信息）

**判断依据**：Alpha_20 + Amount_Share_MA5（基于分位数转换）  
**目的**：判断板块的成长性和产业趋势状态

| Alpha_20 分位数 | Amount_Share_MA5 分位数 | 位置判断 | 含义 |
|-----------------|------------------------|----------|------|
| > 80% | > 80% | 高位区 | 板块过热，产业趋势可能见顶 |
| 60%~80% | 60%~80% | 中高位区 | 板块偏热，产业趋势仍有机会 |
| 40%~60% | 40%~60% | 中位区 | 板块正常，产业趋势稳定 |
| 20%~40% | 20%~40% | 低位区 | 板块低估，产业趋势可能有问题 |
| < 20% | < 20% | 冰点区 | 板块被遗忘，无人关注 |

**【新增】阈值说明**：
- 以上阈值使用**自身历史分位数**（近60日滚动窗口），而非绝对值。例如“Alpha_20 > 80%”表示当前 Alpha_20 大于过去60日中80%的时期。
- 初始阶段可用固定绝对值（如原文档中的+8%等）快速测试，但最终版本必须采用分位数，以解决 ETF 口径下绝对值不可达的问题。
- 每个 ETF 独立计算自身分位数，实现分板块动态阈值。

### 5.2 第二层：动能（趋势方向）

**判断依据**：Alpha_5 + MA5_Slope + Close vs MA5  
**目的**：判断短期趋势方向和强度

| Alpha_5 分位数 | MA5_Slope | Close vs MA5 | 动能判断 |
|----------------|-----------|--------------|----------|
| > 70% | > 0 | Close > MA5 | **强势向上** |
| > 70% | < 0 | Close < MA5 | **强势向下** |
| 50%~70% | > 0 | Close > MA5 | **偏强向上** |
| 50%~70% | < 0 | Close < MA5 | **偏强向下** |
| 30%~50% | 任意 | 任意 | **中性震荡** |
| < 30% | < 0 | Close < MA5 | **弱势向下** |
| < 30% | > 0 | Close > MA5 | **弱势反弹** |

**【新增】阈值说明**：Alpha_5 的分位数基于过去60日计算，MA5_Slope 符号和 Close vs MA5 仍用原始值。

### 5.3 第三层：资金行为（短期资金动向）

**判断依据**：Amount_Share_Change（分位数） + Bias_20 + Pct + 历史分位  
**目的**：判断短期资金的进出行为

| 优先级 | 条件 | 资金行为判断 | 含义 |
|--------|------|--------------|------|
| 1 | Pct < -3% + Amount_Share > 历史80分位 | **恐慌出逃** | 资金不计成本离场 |
| 2 | Amount_Share_Change > 历史80分位 + Pct > 0 | **放量启动** | 资金进场 |
| 3 | Amount_Share 从近20日高点回落 ≥ 20%（绝对值） | **资金撤退** | 资金逐步流出 |
| 4 | Bias_20 > 8% + Pct > 0 | **加速赶顶** | 短期涨幅过大 |
| 5 | Bias_20 < -8% + Pct < 0 | **超跌反弹** | 可能触底 |
| 6 | 以上都不满足 | **横盘整理** | 正常状态 |

**【新增】阈值说明**：
- 本节中“历史80分位”统一指：以该 ETF 对应指标在**过去60个交易日滚动窗口**内计算的80%分位点（仅使用截至当日已知数据）。
- `Amount_Share > 历史80分位` 表示当前资金关注度处于近60个交易日高位区间。
- `Amount_Share_Change > 历史80分位` 表示资金热度增幅处于近60个交易日高位区间。
- 其余条件中的绝对值阈值（如 -3%、20%、8%、-8%）为初始值，后续也可考虑转换为分位数（如 Pct 处于历史极低分位等），但考虑到直观性，暂保留绝对值，待回测后再优化。

---

## 六、操作建议生成（阶段3）【修订】

本节整合最新的趋势判断和资金行为分析逻辑，采用**分层判断 + 分场景处理**的方式生成操作建议。

### 6.1 趋势判断（新增）

**目的**：确定板块（ETF）当前的中期趋势方向，为后续操作定调。

**输入**：
- 收盘价（close）
- 20日均线（MA20）
- MA20 斜率（过去5日变化）

**判断逻辑**：

```python
def get_trend(ma20, ma20_slope, close):
    """
    趋势判断
    - 上升趋势：MA20斜率 > 0 且 收盘价 > MA20
    - 下降趋势：MA20斜率 < 0 且 收盘价 < MA20
    - 横盘整理：其他情况
    """
    if ma20 is None or ma20_slope is None:
        return "横盘整理"

    if ma20_slope > 0 and close > ma20:
        return "上升趋势"
    if ma20_slope < 0 and close < ma20:
        return "下降趋势"
    return "横盘整理"
```

**输出**：趋势标签（上升趋势、下降趋势、横盘整理）

---

### 6.2 K线形态识别（新增）

**目的**：识别单日K线形态，辅助判断多空力量对比。

**输入**：开盘价、最高价、最低价、收盘价（OHLC）

**判断逻辑**：

```python
def identify_candle(open, high, low, close):
    """
    识别K线形态
    返回：'十字星', '锤子线', '射击之星', '大阳线', '大阴线', '小阳线', '小阴线', '普通K线'
    """
    body = abs(close - open)
    upper = high - max(open, close)
    lower = min(open, close) - low
    total = high - low

    if total == 0:
        return "十字星"

    # 十字星：实体很小（涨跌幅 < 1%）
    body_pct = body / open * 100 if open > 0 else 0
    if body_pct < 1.0:
        # 进一步判断上下影线
        if lower > upper * 2 and lower > body * 0.5:
            return "锤子线"  # 底部长下影（可能止跌）
        if upper > lower * 2 and upper > body * 0.5:
            return "射击之星"  # 顶部上影线（可能见顶）
        return "十字星"

    # 大阳线/大阴线：涨幅/跌幅 > 3%
    if body_pct > 3.0:
        return "大阳线" if close > open else "大阴线"

    # 小阳线/小阴线：涨跌幅 < 1.5%
    if body_pct < 1.5:
        return "小阳线" if close > open else "小阴线"

    return "普通K线"
```

**十字星的特殊含义**：
- **上升趋势**中出现十字星 → ⚠️ 预警信号，可能向下转折
- **下降趋势**中出现十字星 → ✅ 关注信号，可能止跌企稳
- **横盘整理**中出现十字星 → ➡️ 观望，等待方向明确

---

### 6.3 资金行为判断（重新设计）

**目的**：结合成交量和K线形态，识别短期资金动向，**分场景处理**。

**输入**：
- 成交额（amount）
- 3日均量（amount_ma3）
- 涨跌幅（pct）
- 趋势标签（trend）
- K线形态（kline_type）
- 近期最高价、最低价（用于横盘突破判断）

**判断逻辑（按优先级）**：

```python
def get_fund_behavior(trend, amount, amount_ma3, pct, kline_type, recent_high, recent_low):
    volume_ratio = amount / amount_ma3 if amount_ma3 > 0 else 1.0

    # ---------- 横盘趋势单独处理 ----------
    if trend == "横盘整理":
        # 向上突破
        if close > recent_high and volume_ratio > 1.3 and pct > 1:
            return "放量启动"
        # 向下突破
        if close < recent_low and volume_ratio > 1.3 and pct < -1:
            return "恐慌出逃"
        # 缩量
        if volume_ratio < 0.8:
            return "资金撤退"
        return "横盘整理"

    # ---------- 上升趋势 ----------
    if trend == "上升趋势":
        # 放量启动（需要一定涨幅）
        if pct > 1 and volume_ratio > 1.3:
            if kline_type in ["大阳线", "小阳线"]:
                return "放量启动"

        # 加速赶顶
        if kline_type in ["射击之星", "大阴线"]:
            return "加速赶顶"
        if volume_ratio > 1.3 and pct < 0:
            return "加速赶顶"
        if 0 <= pct < 1 and volume_ratio > 1.2:
            return "加速赶顶"  # 放量滞涨

        # 资金撤退
        if volume_ratio < 0.8 and pct < 1:
            return "资金撤退"

        # 十字星预警
        if kline_type == "十字星":
            return "十字星预警"

        return "正常上升"

    # ---------- 下降趋势 ----------
    if trend == "下降趋势":
        # 恐慌出逃
        if pct < -2 and volume_ratio > 1.3:
            return "恐慌出逃"

        # 缩量企稳
        if volume_ratio < 0.7 and pct > -1:
            if kline_type in ["锤子线", "十字星", "小阳线"]:
                return "缩量企稳"

        # 十字星预警
        if kline_type == "十字星":
            return "十字星预警"

        return "正常下降"

    return "未知"
```

**输出**：资金行为标签（放量启动、恐慌出逃、加速赶顶、缩量企稳、放量滞涨、资金撤退、十字星预警、横盘整理等）

---

### 6.4 操作建议映射表（新版本）

**核心思路**：融合**趋势标签 + 资金行为标签**，生成操作建议。

| 趋势 | 资金行为 | 操作建议 | 说明 |
|------|----------|----------|------|
| **上升趋势** | 放量启动 | 积极建仓/持股待涨 | 主升阶段，资金确认 |
| 上升趋势 | 正常上升 | 持有观察 | 健康上涨，继续持有 |
| 上升趋势 | 加速赶顶 | 分批止盈 | 涨速过快或放量滞涨，注意风险 |
| 上升趋势 | 资金撤退 | 逐步减仓 | 量价背离，涨势放缓 |
| 上升趋势 | 十字星预警 | 谨慎观望 | ⚠️ 可能转折，不急于操作 |
| 上升趋势 | 恐慌出逃 | 果断离场 | 突发利空，快速离场 |
| **下降趋势** | 恐慌出逃 | 果断离场/观望 | 加速下跌，不参与 |
| 下降趋势 | 正常下降 | 观望/空仓 | 等待企稳 |
| 下降趋势 | 缩量企稳 | 关注企稳 | ✅ 可能止跌，关注但不急于买入 |
| 下降趋势 | 十字星预警 | 关注企稳 | 多空均衡，观察止跌信号 |
| **横盘整理** | 放量启动（向上突破） | 轻仓试探 | 突破信号，先试探 |
| 横盘整理 | 放量启动（向下突破） | 观望 | 跌破观望 |
| 横盘整理 | 横盘整理 | 小仓埋伏 | 等待方向选择 |
| 横盘整理 | 资金撤退 | 观望 | 资金流出，不参与 |

**优先级说明**：
1. **放量启动**：最优先捕捉的信号
2. **加速赶顶/恐慌出逃**：风险信号，及时处理
3. **十字星预警**：预警信号，不直接触发买卖
4. **正常上升/正常下降**：默认状态

---

### 6.5 归因说明格式

**格式**：
```
以{基准}为基准，{趋势归因}，{动能归因}，{资金行为归因}。
```

**示例**：
```
以创业板为基准，MA20向上且股价在均线之上（上升趋势），5日超额收益+5%（短期强势），资金热度相对5日均值增长30%（放量进场）。
```

---

### 6.6 历史乖离率对比（保留）

| 当前偏离 | 历史极值 | 输出示例 |
|----------|----------|----------|
| +11% | +12% | 当前偏离 +11%，历史极值 +12%，接近历史高位，注意回调风险 |
| +8% | +12% | 当前偏离 +8%，历史极值 +12%，未达极值，可能还有上涨空间，但也注意回调风险 |
| -7% | -8% | 当前偏离 -7%，历史极值 -8%，接近历史低位，关注反弹机会 |
| -4% | -8% | 当前偏离 -4%，历史极值 -8%，未达极值，仍有下跌风险 |

---

### 6.7 待解决问题（TODO）

**本节中暂未实现的功能，标记为后期扩展**：

1. **市场情绪过滤**
   - 当前方案：暂未实现
   - 后期方案：引入全市场涨跌家数比、成交额变化等，在极端行情下修正信号
   - 优先级：低

2. **风险等级评估**
   - 当前方案：暂未实现
   - 后期方案：基于Alpha和Amount_Share的分位数评估风险等级，改为"风险提示"（不控制仓位）
   - 优先级：低

3. **横盘高低点定义**
   - 当前方案：待定（5天或20天）
   - 后期方案：通过回测测试5天vs20天的效果
   - 优先级：中

4. **超跌反弹**
   - 当前方案：暂未实现
   - 后期方案：在错杀板块讨论时再设计
   - 优先级：低

---

## 七、回测框架与阈值优化（阶段4-5）

### 7.1 回测目标
- 验证判断逻辑的有效性
- 调优各阈值参数（分ETF独立优化，采用分位数法）
- 评估不同市场状态下的表现

### 7.2 回测配置

| 项目 | 说明 |
|------|------|
| 起始日期 | 2025-05-19 |
| 回测频率 | 每日 |
| 预测周期 | 3日、5日 |
| 评价指标 | 准确率（未来N日涨跌与建议方向一致的比例）、各建议准确率、胜率、平均收益 |

### 7.3 准确率定义（针对不同建议）

| 操作建议 | 正确标准（未来3日） |
|----------|----------------------|
| 持股待涨、积极建仓、低吸机会、持有 | 涨幅 > 0（或 > ETF自身波动率阈值） |
| 分批止盈、逐步减仓 | 出现冲高回落（最高点 > 入场价5%但收盘回落）或跌幅 > 2% |
| 果断离场、果断止损、坚决回避 | 跌幅 > 2% |
| 观望、等待、小仓埋伏 | 不设定正误，或定义为波动在±2%内 |

### 7.4 阈值优化方法 【修订】

1. **分位数法**：对每个 ETF，统计各指标（Alpha_20, Amount_Share, Alpha_5, Amount_Share_Change, Bias_20 等）的历史分布（建议用滚动60日窗口），计算 20%、40%、60%、80% 分位数作为阈值边界。
2. **映射规则**：将原判断逻辑中的绝对值条件替换为分位数条件（如 Alpha_20 > 80% 对应“高位区”）。
3. **自动搜索（可选）**：可编写脚本遍历不同分位点组合，选择使回测准确率最高的组合，但需注意过拟合风险。
4. **分ETF独立配置**：每个ETF生成一套独立的阈值配置文件（如 `config_etf.py`）。

### 7.5 输出报告
- 总体准确率
- 各信号准确率
- 各ETF准确率
- 胜率、平均收益、夏普比率（阶段8加入）

**【新增】7.6 基准对照建议**
除准确率外，建议增加两个基准对照：
- **随机预测**：与随机猜测（如随机生成买入/卖出信号）的准确率对比。
- **持有指数**：与简单持有该ETF（或基准指数）的收益率对比，验证策略能否产生超额收益。
这有助于识别样本偏差，避免被虚假高准确率误导。

---

## 八、手动标记功能（阶段6）

- 前端页面可选择日期和ETF
- 展示当天该ETF的判断结果（位置、动能、资金、操作建议、归因）
- 展示该ETF未来5日K线图
- 提供“正确”、“错误”、“不确定”三个标记按钮
- 标记结果存入后端（JSON或数据库）

### 8.2 用途
- 收集人工反馈，用于后续阈值调优
- 发现模型的系统性偏差
- 为机器学习积累标注数据


---

## 九、后续扩展规划

### 9.1 市场情绪因子（阶段7）
- 引入全市场成交额 MA5、涨跌家数比、连板高度等
- 定义市场状态：强势上涨/震荡/下跌/恐慌
- 作为顶层过滤器：市场恐慌时，所有买入建议降级或过滤

### 9.2 初步交易策略（阶段8）
- 实现简单的状态机管理持仓
- 定义信号到开平仓动作的映射
- 模拟交易，计算收益率曲线、夏普比率、最大回撤

### 9.3 新闻因子（阶段9）
- 获取板块相关新闻标题，用情感分析打分（-2~+2）
- 作为修正因子，调整操作建议置信度或直接升降级
- 需解决未来函数问题（确保新闻时间与K线对齐）

### 9.4 扩展因子（阶段9）
- 北向资金净流入
- 融资余额变化
- 美股映射（相关指数隔夜涨跌）

### 9.5 从ETF过渡到板块/个股（阶段10）
- 替换数据源为真实板块指数（如申万二级行业）
- 重新优化阈值
- 支持个股（需单独调整阈值）

---

## 十、分阶段开发计划（基于ETF）

| 阶段 | 名称 | 核心任务 | 产出 | 预计周期 |
|------|------|----------|------|----------|
| 0 | 数据准备 | 获取ETF及指数日线数据，存储为本地文件 | 原始数据文件 | 1周 |
| 1 | 动态基准+指标计算 | 实现基准选择，计算所有基础指标 | 指标DataFrame | 1周 |
| 2 | 三层判断（硬阈值） | 实现位置、动能、资金行为判断 | 每日标签 | 3天 |
| 3 | 操作建议+输出 | 生成操作建议、归因、乖离对比 | 最终输出表格 | 3天 |
| 4 | 单ETF回测 | 回测框架，输出准确率等统计 | 回测报告 | 1周 |
| 5 | 阈值优化 | 分ETF动态阈值优化 | 新阈值配置 | 1周 |
| 6 | 手动标记前端 | 日期选择、K线、标记功能 | 交互页面 | 1周 |
| 7 | 市场情绪因子 | 引入大盘状态过滤 | 修正后的建议 | 1周 |
| 8 | 交易策略 | 状态机模拟交易 | 交易记录、曲线 | 1周 |
| 9 | 新闻+其他因子 | 逐步加入新闻、北向等 | 多因子打分系统 | 2周 |
| 10 | 扩展板块/个股 | 迁移至真实板块和个股 | 完整系统 | 2周 |

**建议先完成阶段0-4**，形成最小可行产品（MVP），验证核心逻辑后再逐步叠加。
---

## 十一、技术要点

### 11.1 文件结构（更新）

```
a-stock-monitor/
├── data/                         # 原始数据存放
│   ├── etf_daily/                # ETF日线数据
│   ├── index_daily/              # 指数日线数据
│   └── market/                   # 全市场数据
├── sector_lifecycle/              # 核心逻辑包
│   ├── __init__.py
│   ├── indicators.py             # 指标计算
│   ├── benchmark.py              # 动态基准选择（含重算频率控制）
│   ├── quantile_utils.py         # 分位数计算工具 【新增】
│   ├── position.py               # 位置判断（基于分位数）
│   ├── momentum.py               # 动能判断
│   ├── capital.py                # 资金行为判断
│   ├── advice.py                 # 操作建议生成
│   └── config.py                 # 阈值配置（支持分ETF、分位数）
├── backtest/                      # 回测相关
│   ├── backtest_engine.py        # 回测引擎
│   ├── threshold_optimizer.py    # 阈值优化（分位数搜索）
│   └── report.py                 # 报告生成
├── web/                           # 前端及API
│   ├── server.js                  # API服务（已有）
│   └── public/
│       └── index.html             # 前端页面（含手动标记）
└── docs/                          # 文档
    └── requirements_v2.2.md       # 本需求文档
```

### 11.2 关键注意事项
- **数据对齐**：确保所有时间序列按交易日对齐，无缺失。
- **未来函数**：回测中严格避免使用未来数据（如计算历史极值时，只能用截止当天的数据）。
- **信号重叠**：在未实现交易策略前，仅评价单日信号；实现交易策略后，需定义状态机处理连续信号。
- **阈值动态化**：阈值应定期（如每月）基于最新历史数据重新计算分位数，以适应市场变化。回测时则按当时已知信息计算。

---

## 十二、验收标准

（同 v2.1，略）
### 阶段0-4 验收
- [ ] 能正确获取并存储指定ETF和指数数据
- [ ] 动态基准选择结果合理（如半导体选科创50）
- [ ] 所有指标计算正确（抽样验证）
- [ ] 三层判断标签符合直观（抽查几个典型交易日）
- [ ] 操作建议映射正确，归因文本通顺
- [ ] 回测脚本可运行，输出初步准确率统计

### 阶段5-6 验收
- [ ] 阈值优化后准确率有明显提升（对比硬阈值）
- [ ] 前端页面可切换日期，显示判断结果和K线
- [ ] 标记功能正常工作，数据持久化

### 阶段7-10 验收
- [ ] 市场情绪因子有效过滤极端信号
- [ ] 交易策略模拟有正期望收益
- [ ] 新闻因子正确修正建议
- [ ] 系统可平滑迁移至板块/个股

---
